namespace Asteroids {
  public delegate bool KeyPressed(ControlKeys key);
  public enum ControlKeys {
    ENTER,
    THRUST,
    LEFT,
    RIGHT,
    SHOOT,
    LASER,
    PAUSE,
    QUIT,
    DEBUG
  }
}
